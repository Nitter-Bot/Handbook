\documentclass[10pt,landscape,a4paper,oneside]{article}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{listings}
\usepackage{amssymb, amsmath}
\usepackage[usenames,dvipsnames]{xcolor}
\usepackage{multicol}
\usepackage{fancyhdr}
\usepackage{geometry}
\usepackage{titlesec}
\usepackage{hyperref}

% --- Configuración de Márgenes (Máximo aprovechamiento) ---
\geometry{top=1cm, bottom=1cm, left=0.8cm, right=0.8cm, headsep=0.2cm}

% --- Multicols ---
\setlength{\columnsep}{0.5cm}      % Espacio entre columnas
\setlength{\columnseprule}{0.1pt}  % Línea divisoria fina

% --- Estilo de Página ---
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\textbf{:Alianza de Autismo de Puerto Rico}}
\fancyhead[R]{Page \thepage} % Solo "Page X" como pediste
\renewcommand{\headrulewidth}{0.2pt}

% --- Títulos Compactos ---
\definecolor{darkblue}{rgb}{0,0,0.5}

% Secciones: Barra azul sutil
\titleformat{\section}{\color{darkblue}\normalfont\normalsize\bfseries}{\thesection}{1em}{}[{\titlerule[0.5pt]}]
\titlespacing*{\section}{0pt}{2pt}{2pt}

% Subsecciones: Texto en negrita pequeño
\titleformat{\subsection}{\normalfont\scriptsize\bfseries}{\thesubsection}{0.5em}{}
\titlespacing*{\subsection}{0pt}{1pt}{0pt}

% --- Configuración de C++ (Tu estilo pero compacto) ---
\definecolor{commentgreen}{rgb}{0,0.4,0}
\lstdefinestyle{C++}{
    language=C++,
    basicstyle=\ttfamily\scriptsize, % Tamaño ideal para 3 columnas
    keywordstyle=\color{darkblue}\bfseries,
    commentstyle=\color{commentgreen},
    stringstyle=\color{magenta},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=4pt,
    breaklines=true,
    tabsize=2,
    showstringspaces=false,
    columns=fullflexible,
    keepspaces=true,
    xleftmargin=10pt,
    frame=none
}

% --- TUS COMANDOS ---

% 1. Tu comando original (para que no rompa lo que ya tienes)
\newcommand\cppfile[1]{
    \lstinputlisting[style=C++]{#1}
}

% 2. La "solución chida": Agrega título al índice, a la página y el código de un tiro
% Uso: \algo{Nombre del Algoritmo}{ruta/archivo.cpp}
\newcommand{\algo}[2]{
    \subsection{#1} 
    \cppfile{#2}
}

\newcommand{\nbtitle}[1]{{\centering\Large\textbf{#1}\par}\vspace{0.2cm}}
